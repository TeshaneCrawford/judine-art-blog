---
import GalleryIndex from '../../components/GalleryIndex.astro';

const allowedFolders = ['Caribbean', 'United States', 'Europe'];
---

<GalleryIndex title="Travels" basePath="travels" allowedFolders={allowedFolders} />
