---
import GalleryIndex from '../../components/GalleryIndex.astro';

const allowedFolders = ['Winter', 'Fall', 'Summer', 'Spring', 'Sunsets'];
---

<GalleryIndex title="Nature" basePath="nature" allowedFolders={allowedFolders} />
